"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const utils_1 = require("../utils");
const Token_1 = __importDefault(require("./Token"));
const wrapToken = (token) => {
    if (token instanceof Token_1.default) {
        return token;
    }
    return new Token_1.default(token);
};
class KeystoreAuthenticator {
    constructor(keystore) {
        this.keystore = keystore;
    }
    async createToken(timestamp) {
        const token = await this.keystore.createAuthToken({
            timestampNs: timestamp ? (0, utils_1.dateToNs)(timestamp) : undefined,
        });
        return wrapToken(token);
    }
}
exports.default = KeystoreAuthenticator;
//# sourceMappingURL=KeystoreAuthenticator.js.map